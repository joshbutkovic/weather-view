import React from 'react';
import PropTypes from 'prop-types';

const BackToSearchButton = ({onClick}) => {
return (
            <div>

            </div>
        );
}

BackToSearchButton.propTypes = {};

export default BackToSearchButton;
